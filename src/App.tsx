// App.tsx is no longer the entrypoint — see src/router.tsx and src/main.tsx.
// Route tree: / → WebsitePage, /admin → PortalLogin, /admin/staff → StaffLogin,
// /admin/resident → ResidentPortalPage, /admin/guest → GuestPortalPage,
// /admin/staff/dashboard → StaffDashboard
export {};
